<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/MovieListFragment">

    <fragment
        android:id="@+id/MovieListFragment"
        android:name="com.example.movieList.fragment.MovieListFragment"
        android:label="fragment_image_display"
        tools:layout="@layout/fragment_movies_list">
        <action
            android:id="@+id/action_NewsListFragment_to_movieDetailFragment"
            app:destination="@id/movieDetailFragment" />
    </fragment>
    <fragment
        android:id="@+id/movieDetailFragment"
        android:name="com.example.movieList.fragment.MovieDetailFragment"
        android:label="MovieDetailFragment" />

</navigation>